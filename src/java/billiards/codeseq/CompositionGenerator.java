package billiards.codeseq;

/*
// http://jeromekelleher.net/partitions.php
// Other helpful website
public final class CompositionGenerator {
    private int k;
    private ArrayList<Integer> a;

    // arrays in the paper are 1 indexed, but our arrays are 0 indexed
    // so we need to fix that
    // AccelGen with m = sigma = 1
    public CompositionGenerator(final int n) {
        this.k = 1;
        this.a = new ArrayList<>(Collections.nCopies(n + 1, 0));
        this.a.set(1, n);
    }

    public boolean hasNext() {
        return this.k == 0;
    }

    public int

    // http://stackoverflow.com/questions/10244180/python-generating-integer-partitions



}
*/
