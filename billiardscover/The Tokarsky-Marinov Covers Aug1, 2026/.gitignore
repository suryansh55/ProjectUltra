# Build outputs and caches
build
build-java8
# .app staging dir for package-cover-mac.sh (holds a full copy of the bundled JRE)
build-dmg
bin
.gradle

# Cover data (large; generated / distributed separately)
coversfolder

# IDE
.idea
.settings
.classpath
.project

# macOS / JVM crash dumps
.DS_Store
*.hprof
